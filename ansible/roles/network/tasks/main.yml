- name: Create db network
  docker_network:
    name: db_network
    driver: bridge
    state: present

- name: Create proxy network
  docker_network:
    name: proxy_network
    driver: bridge
    state: present