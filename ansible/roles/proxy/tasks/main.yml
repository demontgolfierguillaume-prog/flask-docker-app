- name: Run proxy container
  docker_container:
    name: proxy
    image: guillaumedmg/tp-devops-httpd:latest
    pull: true
    networks:
      - name: app-network
    env:
      APILINK: "http://backend:8080"
    ports:
      - "80:80"
